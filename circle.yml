machine:

  pre:
    - sudo apt-get update
    - sudo apt-get --reinstall install -qq language-pack-ja
    - sudo update-locale LANG=ja_JP.UTF-8

  timezone: Asia/Tokyo

  environment:
    LANG: ja_JP.UTF-8
    LC_ALL: ja_JP.UTF-8

